NEXT_PUBLIC_APP_NAME=KKK

# When running via Docker Compose, this is injected automatically.
# For local-only dev (no Docker), use the value below.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kkk_db"